spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/Crab2Died/spring-cloud-config-dev/
#          username:
#          password:
server:
  port: 8899